---
phase: 01-foundation-and-domain-contract
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - frontend/src/types/book.ts
  - frontend/src/api/client.ts
  - frontend/src/validation/book.ts
  - frontend/src/App.tsx
  - frontend/src/api/client.test.ts
  - frontend/src/validation/book.test.ts
  - frontend/vite.config.ts
  - README.md
autonomous: true
requirements: [API-01, API-03, BOOK-05, BOOK-06]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The React runtime has typed functions for health, create, and read against the same /api/v1 contract exposed by FastAPI."
    - "Frontend immediate validation mirrors the backend's trimming, year, length, and ISBN canonicalization rules while the API remains authoritative."
    - "The browser's configured development origin can reach the API without wildcard CORS, and client errors expose field messages without parsing framework internals."
  artifacts:
    - path: "frontend/src/validation/book.ts"
      provides: "Client-side mirror of the Phase 1 book validation and canonicalization policy"
    - path: "frontend/src/api/client.test.ts"
      provides: "Typed fetch/error behavior tests for the API boundary"
    - path: "frontend/src/validation/book.test.ts"
      provides: "Frontend/backend policy parity cases"
  key_links:
    - from: "frontend/src/validation/book.ts"
      to: "frontend/src/types/book.ts"
      via: "validation accepts the typed complete book payload"
      pattern: "BookInput"
    - from: "frontend/src/api/client.ts"
      to: "frontend/src/validation/book.ts"
      via: "client submits normalized payloads and maps stable field errors"
      pattern: "VALIDATION_ERROR"
    - from: "frontend/src/api/client.ts"
      to: "backend/app/main.py"
      via: "base URL and endpoint paths target the documented FastAPI contract"
      pattern: "api/v1"
---

<objective>
Make the React runtime a reliable consumer of the stabilized Phase 1 contract, including typed API functions, mirrored validation feedback, and visible health/tracer error behavior.

Purpose: API-01 and BOOK-06 require the frontend and backend to agree on field names, normalized values, and error locations before the full collection experience begins in Phase 3. API-03 requires the browser origin/configuration seam to be explicit and testable.
Output: typed native-fetch client, client validation mirror, focused Vitest coverage, and documented browser/API configuration.
</objective>

## Artifacts this phase produces

- `frontend/src/types/book.ts` for shared request/response/error shapes.
- `frontend/src/api/client.ts` for one fetch/error boundary with health/create/read functions.
- `frontend/src/validation/book.ts` with matching year, text, and ISBN policy.
- `frontend/src/App.tsx` with visible loading, success, and field/general error output for the tracer.
- Vitest tests proving client-side behavior and canonicalization examples.

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-domain-contract/01-RESEARCH.md sections "Pattern 1: Contract-first schemas", "Pattern 4: Explicit CORS configuration", "Open Questions", and "Phase 1 verification policy"
@.planning/research/ARCHITECTURE.md sections "API Contract" and "UI Contract and Data Flow"
@.planning/phases/01-foundation-and-domain-contract/01-02-SUMMARY.md
@.claude/CLAUDE.md
@/home/sjbok/.config/opencode/gsd-core/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed browser contract and mirrored validation tests</name>
  <read_first>frontend/src/types/book.ts, frontend/src/api/client.ts, frontend/src/App.tsx, frontend/package.json, backend/app/books/schemas.py, backend/app/books/validation.py, backend/tests/test_contract.py, .planning/REQUIREMENTS.md</read_first>
  <files>frontend/src/types/book.ts, frontend/src/api/client.ts, frontend/src/validation/book.ts, frontend/src/api/client.test.ts, frontend/src/validation/book.test.ts, frontend/vite.config.ts</files>
  <behavior>
    - Test 1: client validation trims title, author, and genre and rejects blank-after-trim and overlong values with field keys.
    - Test 2: client validation accepts year 1 and the current year, rejects zero/future/non-integer values, and matches backend error field names.
    - Test 3: client ISBN validation accepts separator-bearing valid ISBN-10/ISBN-13 and ISBN-10 X, returning separator-free canonical text without ISBN-10 conversion; invalid check digits and misplaced X fail.
    - Test 4: API client parses a successful JSON response, maps the stable `error.fields` envelope, and does not try to parse a 204 response.
  </behavior>
  <action>Define `Book`, `BookInput`, and `ApiError` TypeScript types using the backend's exact snake_case field names. Implement one `apiClient` module around native `fetch` with a configurable `VITE_API_BASE_URL` defaulting to `http://localhost:8000`, JSON parsing, non-2xx conversion into the stable error type, explicit no-body handling, and functions for health, create, and get-by-ID. Add `frontend/src/validation/book.ts` that mirrors backend policy exactly: trim required text, enforce title/author 255 and genre 100, accept year 1 through the current calendar year, remove spaces/hyphens from ISBN, validate ISBN-10/13 check digits including final-position X, and retain ISBN-10 rather than converting it. Write Vitest tests for the parity matrix and client error semantics before finalizing implementation. Configure Vitest through the existing Vite setup without adding a state/cache or HTTP-client library. This implements API-01, BOOK-05, and BOOK-06 on the browser side.</action>
  <verify>
    <automated>npm --prefix frontend test -- --run</automated>
    <automated>npm --prefix frontend run build && npm --prefix frontend exec tsc -- --noEmit</automated>
  </verify>
  <acceptance_criteria>Typed client functions target `/api/v1`, stable API errors become field-aware `ApiError` values, successful responses retain server field names, validation parity tests cover the documented policy, and the frontend builds with no TypeScript errors.</acceptance_criteria>
  <done>The React code has a tested, centralized contract boundary and mirrors the authoritative backend validation without duplicating network logic in components.</done>
</task>

<task type="auto">
  <name>Task 2: Connect the React shell to live readiness and tracer responses</name>
  <read_first>frontend/src/App.tsx, frontend/src/api/client.ts, frontend/src/types/book.ts, frontend/src/validation/book.ts, frontend/vite.config.ts, docker-compose.yml, backend/app/main.py, .planning/phases/01-foundation-and-domain-contract/01-02-SUMMARY.md</read_first>
  <precondition>01-02-SUMMARY.md exists and `GET /api/v1/health` plus the stable validation/error contract pass the backend suite.</precondition>
  <files>frontend/src/App.tsx, frontend/vite.config.ts, README.md</files>
  <action>Replace any tracer hardcoding in `App.tsx` with explicit `idle`, `loading`, `success`, and `error` state transitions. On mount, call the typed health function, then exercise the complete tracer create/read path with a valid five-field payload and render the server-returned ID/title/ISBN plus readiness status. Render a useful general error for network or 503 failures and render field-level messages from `ApiError.fields`; do not claim success before the API response. Keep the shell accessible with visible labels for any inputs, stable error IDs, and a live status region, while leaving the full collection form/list/search/edit/delete workflow to its assigned phases. Configure Vite's browser-facing API URL through `VITE_API_BASE_URL` and document that the host browser uses `http://localhost:8000`, not the Compose service DNS name. Verify the exact development origin remains the one allowed by FastAPI CORS. This completes the React half of API-01 and API-03 and visibly exercises BOOK-05/BOOK-06's shared contract.</action>
  <verify>
    <automated>npm --prefix frontend test -- --run && npm --prefix frontend run build</automated>
    <automated>docker compose up -d && for i in $(seq 1 30); do curl -fsS http://localhost:8000/api/v1/health && break || sleep 1; done && curl -i -X OPTIONS http://localhost:8000/api/v1/books -H 'Origin: http://localhost:5173' -H 'Access-Control-Request-Method: POST' -H 'Access-Control-Request-Headers: content-type' | python -c 'import sys; s=sys.stdin.read(); assert "access-control-allow-origin: http://localhost:5173" in s.lower() and "access-control-allow-methods" in s.lower()' && docker compose down</automated>
  </verify>
  <acceptance_criteria>The React shell reaches the live FastAPI health endpoint, renders a real persisted tracer response, distinguishes success from API/network failure, displays stable field errors, builds reproducibly, and passes an exact-origin JSON CORS preflight check.</acceptance_criteria>
  <done>Phase 1 has a browser-consumable and browser-verified API contract ready for Phase 2 CRUD and Phase 3 collection UX.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| React/browser → API client | Browser state and user-entered values cross into HTTP requests. |
| API response → React renderer | Server-provided text and error messages are rendered in the UI. |
| Browser origin → FastAPI CORS | Origin and preflight behavior determine browser access to responses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-09 | Tampering | Client payload builder | medium | mitigate | Normalize and validate before submission, while relying on API authority for bypass resistance. |
| T-01-10 | Information disclosure | Error renderer | medium | mitigate | Consume only the stable non-sensitive error envelope and show generic network/server messages. |
| T-01-11 | Spoofing/Elevation | Browser CORS configuration | medium | mitigate | Use exact configured origin and preflight verification; do not imply CORS supplies authorization. |
| T-01-SC | Tampering | npm/PyPI install seam | high | mitigate | Package-legitimacy checkpoint occurs before any dependency install in Plan 01. |
</table>
</threat_model>

<verification>
Run frontend Vitest and TypeScript build checks, start the local Compose runtime, verify health, perform the exact-origin JSON preflight, and inspect the rendered shell against the live API response. Confirm `VITE_API_BASE_URL` is host-reachable from browser JavaScript.
</verification>

<success_criteria>
The React client and FastAPI API share typed field names, error envelopes, normalization semantics, and explicit browser-origin configuration; automated frontend checks and live CORS/readiness checks pass.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-and-domain-contract/01-03-SUMMARY.md` when done.
</output>
